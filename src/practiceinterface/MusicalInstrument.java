package practiceinterface;

public interface MusicalInstrument {
	
	/*	
	 * 1. Create an interface named MusicalInstrument. 
	 * 
	 * 2. Define methods like play(), tune(), and stop(). 
	 * 
	 * */
	int number = 10; // it is a final variable
	void play();
	void tune();
	void stop();
	
	int sum1(int num1, int num2);
	int sum(int num3, int num4, int num5);
	// if you want to creat a method that return something 
	
	
	

}
